<UserControl x:Class="SimpleEmail.Views.EmailAccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimpleEmail.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto, Auto, Auto, Auto, Auto, *">

        <TextBlock Text="Server Address" />
        <TextBlock Text="Server Port" Grid.Row="1" />
        <TextBlock Text="User" Grid.Row="2" />
        <TextBlock Text="Password" Grid.Row="3" />
        <TextBlock Text="Client Id" Grid.Row="4" />
        <TextBlock Text="Client Secret" Grid.Row="5" />

        <TextBox Text="{Binding ServerAddress}" Grid.Column="1" />
        <TextBox Text="{Binding ServerPort}" Grid.Column="1" Grid.Row="1" />
        <TextBox Text="{Binding User}" Grid.Column="1" Grid.Row="2" />
        <PasswordBox x:Name="PasswordTB" PasswordChanged="PasswordTB_PasswordChanged" Grid.Row="3" Grid.Column="1" />
        <TextBox Text="{Binding ClientId}" Grid.Column="1" Grid.Row="4" />
        <TextBox Text="{Binding ClientSecret}" Grid.Column="1" Grid.Row="5" />

    </Grid>
</UserControl>
