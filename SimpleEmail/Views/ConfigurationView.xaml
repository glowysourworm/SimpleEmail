<UserControl x:Class="SimpleEmail.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SimpleEmail.Views">
    
    <TabControl>

        <!-- Email Accounts -->
        <TabItem Header="Email Accounts">
            <ListBox x:Name="EmailAccountLB" ItemsSource="{Binding EmailAccountConfigurations}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <local:EmailAccountConfigurationView Width="400" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </TabItem>
        
    </TabControl>
</UserControl>
