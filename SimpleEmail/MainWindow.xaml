<Window x:Class="SimpleEmail.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SimpleEmail"
        Title="Simple Email" Height="450" Width="800">
    <Grid>
        <TabControl x:Name="PrimaryTabControl">
            <TabItem Header="Configuration">
                <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto, Auto, Auto ,Auto ,*">

                    <TextBlock Text="Server Address" />
                    <TextBlock Text="Server Port" Grid.Row="1" />
                    <TextBlock Text="User" Grid.Row="2" />
                    <TextBlock Text="Password" Grid.Row="3" />

                    <TextBox Text="{Binding Configuration.ServerAddress}" Grid.Column="1" />
                    <TextBox Text="{Binding Configuration.ServerPort}" Grid.Column="1" Grid.Row="1" />
                    <TextBox Text="{Binding Configuration.User}" Grid.Column="1" Grid.Row="2" />
                    <PasswordBox x:Name="PasswordTB" PasswordChanged="PasswordTB_PasswordChanged" Grid.Row="3" Grid.Column="1" />

                    <Button x:Name="DownloadButton" 
                            Click="DownloadButton_Click" 
                            Content="Download" 
                            Grid.Column="1" 
                            Grid.Row="4" 
                            VerticalAlignment="Bottom"
                            HorizontalAlignment="Right"
                            />

                </Grid>
            </TabItem>
            <TabItem Header="Email">
                <DataGrid x:Name="MailGrid" 
                          ItemsSource="{Binding PrimaryMail}" 
                          IsReadOnly="True"
                          AutoGenerateColumns="False"
                          BorderBrush="Transparent"
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="LightGray"
                          VerticalGridLinesBrush="Transparent"
                          RowHeaderWidth="0"
                          ColumnWidth="*"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="False"
                          CanUserResizeColumns="True"
                          CanUserSortColumns="True"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="False"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"                  
                          VirtualizingPanel.CacheLength="3"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Subject}" Header="Subject" Width="Auto" />
                        <DataGridTextColumn Binding="{Binding From}" Header="From" Width="Auto" />
                        <DataGridTextColumn Binding="{Binding Timestamp}" Header="Date" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
